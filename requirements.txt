torch>=2.0.0
transformers>=4.43.1,<4.44.0
multiaddr>=0.1.1
accelerate>=1.12.0
hf_transfer>=0.1.9
speedtest-cli==2.1.3
huggingface_hub>=0.16.0  # Required for selective model loading
safetensors>=0.3.0  # Required for selective model loading (optional, with fallback)

# Optional dependencies (with fallbacks in code)
async-timeout>=4.0.0  # Optional: used for Python <3.11 (Python 3.11+ uses asyncio.timeout)
bitsandbytes == 0.49.1  # Optional: used for int4/int8 quantization support (compatible with transformers 4.43.1)

